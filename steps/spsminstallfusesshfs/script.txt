#OUTPUT=$(rpm -qa | grep -iq fuse-sshfs) || true
#cho "OUTPUT='${OUTPUT}'"

if rpm -qa | grep -iq fuse-sshfs
then
    echo "Installing fuse-sshfs"
    dnf -y install fuse-sshfs
else
    echo "fuse-sshfs is already installed, skipping."
fi
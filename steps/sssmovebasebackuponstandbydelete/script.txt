DESTINATION_DIRECTORY="postgresqlBaseBackup"

mkdir -vp "${DESTINATION_DIRECTORY}"
cd  "${DESTINATION_DIRECTORY}"

BACKUP_FILE="base.tar.gz"

if [ -f "${BACKUP_FILE}" ]
then
    echo "Removing old base backup."
    rm -v "${BACKUP_FILE}"
fi

mv -v \
    "/pgwal_archive/{peekStandbyServer.hostname}/${BACKUP_FILE}" \
    ./

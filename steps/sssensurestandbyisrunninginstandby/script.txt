if cat "{postgresqlDirectory}/log/*.log" | grep -q 'entering standby mode'
then
    echo "Success, Standby did enter standby mode."
else
    echo "FAILED, Standby did not enter standby mode." >&2
    false
fi
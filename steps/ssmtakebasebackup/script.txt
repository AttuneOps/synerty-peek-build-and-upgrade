DIRECTORY=/pgwal_archive/{peekStandbyServer.hostname}
mkdir -p "${DIRECTORY}/postgresqlBaseBackup"

cd "${DIRECTORY}/postgresqlBaseBackup"

echo "Putting backup in ${DIRECTORY}"

pg_basebackup --pgdata=./ \
    --format=tar \
    --gzip \
    --wal-method=fetch \
    --progress
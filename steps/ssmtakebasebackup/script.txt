DIRECTORY=/pgwal_archive/{peekStandbyServer.hostname}
cd "${DIRECTORY}"

echo "Putting backup in ${DIRECTORY}"

pg_basebackup --pgdata=./ \
    --format=tar \
    --gzip \
    --wal-method=fetch \
    --progress